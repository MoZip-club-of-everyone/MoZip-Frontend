// 모집 폼 생성 화면
// 담당자(담당 브랜치): nayeong

import CustomButton from "@/components/CustomButton";
import CustomFont from "@/components/CustomFont";

export default function Home() {

	return (
		<div>
			모집 폼 생성 화면
			<CustomButton $width="10rem">
				<CustomFont $color="white" $font="1rem">
					안녀엉!
				</CustomFont>
			</CustomButton>
		</div>
	);
}
